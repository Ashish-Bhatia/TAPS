branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ] || [ "$branch" = "develop" ]; then
  echo "BLOCKED: direct commit to $branch. Create a feature branch first."
  exit 1
fi

npx lint-staged
npm run lint --workspaces --if-present
