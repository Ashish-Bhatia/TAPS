import { ComingSoon } from '../../components/ui/ComingSoon';

export default function SyllabusPage() {
  return (
    <ComingSoon
      title="Syllabus"
      note="Per-exam syllabus breakdowns are tracked in the backlog, pending a Syllabus public API."
    />
  );
}
