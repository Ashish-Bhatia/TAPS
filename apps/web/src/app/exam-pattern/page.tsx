import { ComingSoon } from '../../components/ui/ComingSoon';

export default function ExamPatternPage() {
  return (
    <ComingSoon
      title="Exam Pattern"
      note="Per-exam pattern breakdowns are tracked in the backlog."
    />
  );
}
