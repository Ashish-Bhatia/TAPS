import { ComingSoon } from '../../components/ui/ComingSoon';

export default function NcertBooksPage() {
  return (
    <ComingSoon
      title="NCERT Books"
      note="Class-wise, subject-wise book downloads are tracked in the backlog, pending a Book public API."
    />
  );
}
