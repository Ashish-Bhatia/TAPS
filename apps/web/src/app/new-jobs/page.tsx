import { ComingSoon } from '../../components/ui/ComingSoon';

export default function NewJobsPage() {
  return (
    <ComingSoon
      title="New Jobs"
      note="A feed of fresh recruitment notifications (Post records) is tracked in the backlog."
    />
  );
}
