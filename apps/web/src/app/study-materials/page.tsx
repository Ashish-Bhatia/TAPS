import { ComingSoon } from '../../components/ui/ComingSoon';

export default function StudyMaterialsPage() {
  return (
    <ComingSoon
      title="Study Materials"
      note="Subject-wise notes and downloads are tracked in the backlog, pending a StudyMaterial public API."
    />
  );
}
