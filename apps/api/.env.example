# Copy to .env and fill in real values locally. Never commit .env — see
# docs/project-knowledge/06-CODING-STANDARDS.md "Environment & Secrets".
# In deployed environments (Fly.io), set these as secrets, not plain env vars.

# PostgreSQL connection string. Shape below matches Neon's pooled connection
# string (what this project actually uses — see
# docs/runbooks/database-migrations.md) rather than a generic local Postgres
# URL: a "-pooler" host, and both `sslmode=require` and `channel_binding=require`
# in the query string (Neon rejects a plain unencrypted connection). Get the
# real value from the Neon console's "Connection string" panel, or from Fly
# secrets for the deployed environment — never from chat, a commit, or a PR.
DATABASE_URL=postgresql://<user>:<password>@<project>-pooler.<region>.aws.neon.tech/<database>?sslmode=require&channel_binding=require

# Anthropic API key for the AIService boundary (see 05-ARCHITECTURE.md §5).
# Real keys look like `sk-ant-api03-<~100 more characters>` — get one from
# console.anthropic.com. Called for real as of TAPS-4.1
# (AIService.generateQuizFromPaper) — a placeholder value here fails every
# call with a 401 authentication_error, not silently. Also set as a Fly
# secret on `taps-api` for the deployed environment (done as of TAPS-4.2).
ANTHROPIC_API_KEY=sk-ant-api03-replace-with-a-real-key

# OpenAI API key — the TAPS-4.2 fallback provider AIService retries against
# when Anthropic fails in a fallback-eligible way (billing/credit error,
# rate limit, or a 5xx; see docs/adr/012-ai-provider-fallback.md). Real keys
# look like `sk-proj-<~100 more characters>` or `sk-<~48 characters>` — get
# one from platform.openai.com. Also set as a Fly secret on `taps-api`.
OPENAI_API_KEY=sk-proj-replace-with-a-real-key

# Port the NestJS server listens on. Falls back to 8080 if unset (see main.ts).
PORT=8080

# Origin allowed to make cross-origin requests (the web app). Falls back to
# the Next.js dev default (http://localhost:3000) if unset (see main.ts). In
# production this is the real Vercel Production URL for apps/web — exact
# scheme+host, no path, no trailing slash (see docs/runbooks/deploy-api.md
# "CORS Configuration" for the full explanation and a live curl check), e.g.:
#   ALLOWED_ORIGIN=https://taps-web-eta.vercel.app
ALLOWED_ORIGIN=http://localhost:3000

# Signs/verifies the admin JWT (see docs/adr/005-admin-auth-and-validation.md
# and docs/api/auth.md). Any long random string; e.g. generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace-with-a-long-random-string

# bcrypt hash of the single admin password (never the plaintext — see the
# same ADR). Generate with:
#   node -e "require('bcryptjs').hash(process.argv[1], 10).then(console.log)" 'your-chosen-password'
ADMIN_PASSWORD_HASH=$2b$10$replace.with.a.real.bcrypt.hash.................

# Signs/verifies the user-session JWT for apps/api/src/user-auth/ (TAPS-5.1) — a
# deliberately separate secret from JWT_SECRET above, not a shared one, so a leaked
# admin secret can never forge a user token and vice versa (blast-radius separation,
# see docs/adr/014-user-auth-separate-from-admin-auth.md). Any long random string,
# generated the same way as JWT_SECRET.
USER_JWT_SECRET=replace-with-a-different-long-random-string

# TAPS-3.7 (docs/adr/019-tag-based-cache-revalidation.md): base URL of
# apps/web, called server-to-server by RevalidationService after an
# ExamBoard/Post admin write. Falls back to the Next.js dev default if
# unset (see revalidation.service.ts) — mirrors ALLOWED_ORIGIN's pattern
# but in the opposite direction (apps/api calling INTO apps/web, not
# apps/web's browser calling apps/api).
WEB_APP_URL=http://localhost:3000

# Shared secret authenticating this app's server-to-server calls to
# apps/web's POST /api/revalidate — must match apps/web's own
# REVALIDATION_SECRET exactly (see apps/web/.env.example). Any long random
# string, generated the same way as JWT_SECRET. Optional in local dev:
# RevalidationService silently skips revalidation calls when this is unset
# (TAPS-3.7's caching is opt-in infrastructure, not a correctness
# requirement — see docs/adr/019-tag-based-cache-revalidation.md).
REVALIDATION_SECRET=replace-with-a-long-random-string

# TAPS-2.13: Cloudflare R2 (S3-compatible object storage) — StorageService's
# real producer of PastPaper/StudyMaterial/Book fileUrl values via
# POST /storage/upload. All five are required together; StorageService
# throws InternalServerErrorException on first use if any is missing,
# rather than failing app boot (same lazy-check convention as
# ADMIN_PASSWORD_HASH/JWT_SECRET above — most requests never touch storage).
# Get these from the Cloudflare dashboard: R2 > Manage R2 API Tokens for the
# credentials, and the bucket's Settings page for its id and public URL.

# The Cloudflare account id that owns the R2 bucket — forms the S3 API
# endpoint as https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com.
R2_ACCOUNT_ID=replace-with-your-cloudflare-account-id

# R2 API token access key id (scoped to Object Read & Write on this bucket
# only — not a broader Cloudflare API token).
R2_ACCESS_KEY_ID=replace-with-your-r2-access-key-id

# R2 API token secret access key — shown once at creation time in the
# Cloudflare dashboard, never recoverable afterward.
R2_SECRET_ACCESS_KEY=replace-with-your-r2-secret-access-key

# Name of the R2 bucket StorageService uploads into.
R2_BUCKET_NAME=replace-with-your-bucket-name

# Base URL uploaded files are publicly reachable at (the bucket's R2.dev
# subdomain, or a custom domain mapped to it) — StorageService returns
# `${R2_PUBLIC_URL}/<key>` as fileUrl. No trailing slash needed; one is
# stripped if present.
R2_PUBLIC_URL=https://replace-with-your-bucket-public-url
