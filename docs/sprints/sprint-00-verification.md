# Sprint 0 — Adversarial Verification

Run date: 2026-09-09. Every row below reflects a command actually executed in this session
against the repo at commit `21639ef`, not the prior session's self-report. `docs/backlog/BACKLOG.md`
Status was not trusted going in — it was treated as an unverified claim.

Two things came up repeatedly and are stated once here instead of in every row: several checks
in the verification brief tested scope that no existing backlog row ever promised (a Prisma
schema and `/health` route for TAPS-1.2; per-app distinct CI jobs for TAPS-1.8; real consumer
imports for TAPS-1.12/1.13, which were already honestly marked "Ready" not "Done"). Where that's
the case, the table separates "does the story's actual claimed scope hold up" from "does it also
satisfy the extra checks" — a gap in the second is reported as a new backlog item, not used to
retroactively fail a story for something it never claimed.

| Story                                       | Prior Claimed Status               | Verified Status                                                                             | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TAPS-1.1 — apps/web scaffold                | Done                               | **PASS**                                                                                    | `package.json` has real `next@16.3.4`, `react@19.2.3`, `react-dom@19.2.3` (not placeholders); `tsconfig.json` has `"strict": true`. `npm run build -w apps/web` → `✓ Compiled successfully in 12.9s`, `Finished TypeScript in 4.3s`, 4/4 static pages generated, exit 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| TAPS-1.2 — apps/api scaffold                | Done                               | **PASS** on claimed scope (NestJS + Vitest); **new gaps found, not part of original scope** | `@nestjs/core@^12.0.1` and `vitest@^4.1.2` present. `npm run test -w apps/api` → `Test Files 1 passed (1)`, `Tests 1 passed (1)`, exit 0. Started `apps/api` on port 4001: `curl /` → `Hello World!` (HTTP 200, NestJS default controller — not a dedicated health-check route). `curl /health` → `404 {"message":"Cannot GET /health"}`. `ls apps/api/prisma/schema.prisma` → `No such file or directory`. Server killed after test (verified via `curl` connection-refused). **No existing backlog row ever promised Prisma or a `/health` endpoint** — a `/health` endpoint is filed as new row TAPS-1.20 below; a Prisma schema is deliberately not filed (see note below the table) rather than failing TAPS-1.2 for scope it never claimed.                                                                                                                                 |
| TAPS-1.3 — apps/mobile scaffold             | Done                               | **PASS** on claimed scope (Android-first per PRD); **one new gap found**                    | `package.json` has real `expo@~57.0.21`, `react-native@0.86.3`, `typescript@~6.0.3`. `npx tsc --noEmit -p apps/mobile` → exit 0, no output (clean). `npx expo export --platform android --platform ios` → `Android Bundled … (580 modules)`, `iOS Bundled … (582 modules)`, `Exported: dist`, exit 0. `npx expo export` (default, all platforms including web) → **fails**: `CommandError: … don't have the required dependencies installed. Install react-native-web`. `app.json` declares a `web` config block but `react-native-web` was never installed — inconsistent, since the PRD scopes mobile to Android-first with iOS in Phase 2 and says nothing about web. Export artifacts (`apps/mobile/dist/`) deleted after the check.                                                                                                                                          |
| TAPS-1.4 — npm workspaces monorepo          | Done                               | **PASS**                                                                                    | Root `package.json` `workspaces: ["apps/*", "packages/*"]`. `find . -name package-lock.json -not -path '*/node_modules/*'` → exactly one, at root. Full `rm -rf node_modules apps/*/node_modules && npm install` → `added 1145 packages, and audited 1149 packages` with **no `npm error` lines**, exit 0. Re-ran `npm explain next` post-reinstall: still hoists to root (the ADR 001 fix survived a clean reinstall, not just the original install).                                                                                                                                                                                                                                                                                                                                                                                                                            |
| TAPS-1.5 — ESLint standardized              | Done                               | **PASS**                                                                                    | `grep -rl oxlint --include=package.json .` (excluding node_modules) → zero hits. All three apps have an eslint config file (`apps/api/eslint.config.mjs`, `apps/mobile/eslint.config.js`, `apps/web/eslint.config.mjs`). `npm run lint` (root, fans out to all 3 workspaces) → api: `0 errors, 1 warning` (pre-existing `no-unsafe-argument` warning in a generated e2e spec, not a regression); mobile: clean; web: clean. Exit 0.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| TAPS-1.6 — Prettier                         | Done                               | **PASS**, with one latent bug found                                                         | `.prettierrc.json` present and non-empty. Root `package.json` has both `format` and `format:check` scripts. `npm run format:check` run in a from-scratch, CI-order sequence (`npm ci`-equivalent → lint → test → format:check, nothing else) → `All matched files use Prettier code style!`, exit 0. **However**, an earlier run of this same check — after I'd run `next build` for TAPS-1.1 in this same working tree — failed on `apps/web/next-env.d.ts` (a Next.js auto-generated, gitignored file not covered by `.prettierignore`). Confirmed this file is _not_ git-tracked and CI's actual step order (`npm ci` → lint → test → format:check) never triggers Next's codegen, so **CI itself is not affected**, but any developer who has ever run `next dev`/`next build` locally will see `npm run format:check` spuriously fail afterward. Filed as new row TAPS-1.17. |
| TAPS-1.7 — Husky pre-commit                 | Done                               | **PASS** — live-tested                                                                      | `.husky/pre-commit` contents confirmed: `npx lint-staged` then `npm run lint --workspaces --if-present`. **Live test**: on throwaway branch `sprint0-verify-husky-throwaway`, added `apps/api/src/husky-verify-throwaway.ts` with an intentionally unused variable, `git add`, `git commit` → hook ran, ESLint reported `error 'unusedVariable' is assigned a value but never used @typescript-eslint/no-unused-vars`, `npm error code 1`, `husky - pre-commit script failed (code 1)`, commit **rejected** (`git log` confirmed HEAD unchanged at `21639ef`, no new commit). Cleaned up: `git reset --hard`, checked out `main`, deleted the throwaway branch. Repo verified clean afterward (`git status --short` empty).                                                                                                                                                       |
| TAPS-1.8 — GitHub Actions CI                | Done                               | **PASS** on claimed scope ("lint + test on every PR"); **one structural gap found**         | Workflow triggers on `pull_request` to `main`/`develop` and `push` to `main`; runs `npm ci`, `npm run lint --workspaces --if-present`, `npm run test --workspaces --if-present`, `npm run format:check`. `gh run list --limit 10` → **one real run exists** for commit `21639ef`: `completed success … CI main push 34415098125 43s`. This is a real, passing run, not just an unexercised workflow file. **Gap**: the brief asked for "lint+test steps per app with distinct job names" — the workflow uses a single `lint-and-test` job that fans out across workspaces in shared steps, not three separate named jobs. Functionally all three apps are covered and the one real run proves it; the single-job structure just means GitHub's UI won't show separate green/red per app. Filed as new row TAPS-1.18.                                                              |
| TAPS-1.9 — local dev setup doc              | Done                               | **PASS**                                                                                    | Every command in `docs/setup/local-development.md` was cross-checked with `grep` against actual `package.json` scripts (not eyeballed): `dev`/web, `start:dev`/api, `start`+`android`+`ios`+`web`/mobile, `lint`/`format`/`format:check`/`test` at root — all present exactly as documented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| TAPS-1.10 — ADR: monorepo tooling           | Done                               | **PASS**                                                                                    | `docs/adr/001-monorepo-tooling.md` has all four required elements per `07-DOCUMENTATION-STANDARDS.md`: `# 001 — <Title>` heading, `Status: Accepted` inline exactly as specified, then Context / Decision / Consequences content. Minor style deviation: the spec's example shows `Context:`/`Decision:`/`Consequences:` as inline labels, this ADR uses `## Context` / `## Decision` / `## Consequences` headers — all content is present, just structured as sections rather than one-line labels.                                                                                                                                                                                                                                                                                                                                                                              |
| TAPS-1.11 — test scaffolding for web/mobile | Ready (already, not Done)          | **FAIL** (confirms it was correctly never claimed Done)                                     | `npm test -w apps/web` → `npm error Missing script: "test"`, exit 1. `npm test -w apps/mobile` → `npm error Missing script: "test"`, exit 1. No prior-session claim contradicted — backlog already had this as "Ready".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| TAPS-1.12 — packages/types real             | Ready (already, not Done)          | **FAIL** (confirms correct)                                                                 | `find packages/types -type f` → no output, directory is empty. `grep -rn "@taps/types" apps/web apps/api` → zero hits. No prior claim contradicted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| TAPS-1.13 — packages/ui real                | Ready (already, not Done)          | **FAIL** (confirms correct)                                                                 | `find packages/ui -type f` → no output, empty. `grep -rn "@taps/ui" apps/web` → zero hits. No prior claim contradicted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| TAPS-1.14 — branch protection               | _(did not exist as a backlog row)_ | **INDETERMINATE**                                                                           | `gh api repos/Ashish-Bhatia/TAPS/branches/main/protection` → `403 Resource not accessible by integration`. Same for `develop`. The ambient `GITHUB_TOKEN` in this session lacks `admin:repo` scope needed to read branch protection — this proves nothing about whether protection is actually configured, in either direction. Cannot be resolved without a token with repo-admin rights or the founder checking the GitHub UI directly. Added as a new backlog row (not "Done", not silently omitted).                                                                                                                                                                                                                                                                                                                                                                          |
| TAPS-1.15 — env/secrets convention          | _(did not exist as a backlog row)_ | **FAIL** (file doesn't exist yet) / **INDETERMINATE** (secrets)                             | `find . -iname .env.example -not -path '*/node_modules/*'` → no results; `apps/api/.env.example` does not exist (consistent with `docs/setup/local-development.md`'s own statement that no `.env` is needed yet — not a security incident, just an unbuilt item). `.gitignore` correctly has `.env` / `.env.*` with `!.env.example` exception. `git log --all --full-history -- '**/.env' '**/*.env'` → empty, `.env` was never committed in this repo's history. `gh secret list` → `403 Resource not accessible by integration`. `gh codespace secret list` → not a real `gh` subcommand in this CLI version (`unknown command "secret" for "gh codespace"`); could not run as specified.                                                                                                                                                                                       |
| TAPS-1.16 — deploy                          | _(did not exist as a backlog row)_ | **PASS** (confirmed not falsely claimed)                                                    | `grep -in "deploy\|vercel\|fly" docs/backlog/BACKLOG.md` → no hits; no deploy story exists anywhere in the backlog, so nothing is falsely marked done. Added as a new row so it's tracked rather than absent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## New backlog items filed as a result of this verification

Per the instruction not to repair anything in this pass, the following are recorded as new,
separately-scoped backlog rows rather than fixed now:

- **TAPS-1.17** — Fix `.prettierignore` to cover Next.js-generated files (`next-env.d.ts` at minimum) so `format:check` doesn't spuriously fail after a normal local `next dev`/`next build`.
- **TAPS-1.18** — Split CI's single `lint-and-test` job into distinct per-app jobs for clearer pass/fail visibility in GitHub's UI (current single-job setup is functionally proven working — see TAPS-1.8 — this is a structure/observability improvement, not a bug fix).
- **TAPS-1.19** — Add `react-native-web` to `apps/mobile` (or drop the `web` block from `app.json`) so `npx expo export`'s default (all-platforms) mode doesn't fail; not required for the PRD's Android-first scope.
- **TAPS-1.20** — Add a `/health` endpoint to `apps/api` (basic scaffolding gap; TAPS-1.2 never actually promised one). A real Prisma schema is deliberately _not_ filed as a Sprint 0 gap — an empty schema with no models isn't useful, and `05-ARCHITECTURE.md`'s data model belongs with EPIC 2 (Content data model) planning, not repo scaffolding.

## Full command log

All commands were run directly against the working tree at `/workspaces/TAPS`, `git status --short`
was empty before and after (except during the TAPS-1.7 live test, which used a throwaway branch
deleted at the end), and no repair was made to any failing item as part of this pass.
