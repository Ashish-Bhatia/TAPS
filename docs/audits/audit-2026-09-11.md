# TAPS Repository Audit — 2026-09-11

**Scope:** Full read-only audit. No files were modified, created (other than this report), or deleted; no git write operations (commit/push/merge/rebase) were run.

---

## 1. Git State

- **Current branch:** `develop`
- **HEAD commit:** `c5709f141b5d71956d671b39ae988b55969fc4c1` — "Merge pull request #21 from Ashish-Bhatia/feature/TAPS-3-4-search"
- **Working tree:** clean, up to date with `origin/develop`, no uncommitted or untracked files.
- **Tags:** none.

**Local branches:**

| Branch                              | Tip       | Tracking                                                                                                    |
| ----------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------- |
| `develop` (current)                 | `c5709f1` | `origin/develop`, up to date                                                                                |
| `feature/TAPS-1-fix-prepare-script` | `996da16` | `origin/feature/TAPS-1-fix-prepare-script`                                                                  |
| `main`                              | `21639ef` | `origin/main`, **behind 40 commits** (local checkout is stale — `origin/main` itself is current, see below) |

**Remote branches:** `origin/HEAD → origin/main`, `origin/develop`, `origin/feature/TAPS-1-fix-prepare-script`, `origin/main`.

- `origin/main` HEAD: `f9ce73a` — "Merge pull request #22 from Ashish-Bhatia/develop" (main is in sync with develop at the remote; only the **local** `main` branch ref is stale/unpulled — not a repo defect).

**Last 20 commits (develop):**

```
c5709f1 Merge pull request #21 from Ashish-Bhatia/feature/TAPS-3-4-search
9b3055f feat(TAPS-3.4): basic Postgres full-text search
7c63a3b chore(TAPS-3.4): mark In Progress
e4b80fa Merge pull request #20 from Ashish-Bhatia/feature/TAPS-3-3-exam-hub-page
8233260 feat(TAPS-3.3): exam hub page template
7d62d6a chore(TAPS-3.3): mark In Progress
4991f13 Merge pull request #19 from Ashish-Bhatia/feature/TAPS-3-2-nav-shell
97b0e62 feat(TAPS-3.2): apps/web navigation shell
7a872a8 chore(TAPS-3.2): mark In Progress
016854e Merge pull request #18 from Ashish-Bhatia/feature/TAPS-3-1-public-content-api
2d4aae9 feat(TAPS-3.1): public read-only ExamBoard/Post API
f40b25c chore(TAPS-3.1): mark In Progress
442aea0 Merge pull request #16 from Ashish-Bhatia/feature/TAPS-sprint-1-summary
38fc21d docs: propose Sprint 2 backlog (EPIC 3 web app + public content API)
dd678ce docs: Sprint 1 close-out summary
b066dd8 Merge pull request #15 from Ashish-Bhatia/feature/TAPS-1-15-env-example
7f14f75 Merge remote-tracking branch 'origin/develop' into feature/TAPS-1-15-env-example
9d95dac docs(TAPS-1.15): write real apps/api/.env.example shapes
a89dff7 Merge pull request #14 from Ashish-Bhatia/feature/TAPS-2-4-seed-script
beca0d9 Merge remote-tracking branch 'origin/develop' into feature/TAPS-2-4-seed-script
```

---

## 2. Structure

**`apps/` (2 levels):**

```
apps/api   → .env, .env.example, Dockerfile, README.md, dist/, eslint.config.mjs, fly.toml,
             nest-cli.json, node_modules/, package.json, prisma/, src/, test/,
             tsconfig*.json, vitest.config*.ts
apps/mobile → .claude/, .expo/, .gitignore, AGENTS.md, App.tsx, CLAUDE.md, app.json, assets/,
              eslint.config.js, index.ts, package.json, tsconfig.json
apps/web   → .env.example, .gitignore, .next/, AGENTS.md, CLAUDE.md, README.md,
             eslint.config.mjs, next-env.d.ts, next.config.ts, node_modules/, package.json,
             postcss.config.mjs, public/, src/, tsconfig*.json, vitest.config.ts, vitest.setup.ts
```

**`packages/` (2 levels):**

```
packages/types  (empty directory)
packages/ui     (empty directory)
```

**Match against expected layout (`apps/web`, `apps/mobile`, `apps/api`, `packages/types`, `packages/ui`):** ✅ Matches exactly at the top level. `packages/types` and `packages/ui` exist as directories but are genuinely empty (no `package.json`, no source) — consistent with backlog `TAPS-1.12`/`TAPS-1.13`, both still status "Ready" (not started). This is documented, expected drift, not an audit finding.

---

## 3. Backlog Truth Check

Full contents of `docs/backlog/BACKLOG.md` table, story-by-story:

| ID        | Status (as recorded)                                                                                    |
| --------- | ------------------------------------------------------------------------------------------------------- |
| TAPS-1.1  | Done                                                                                                    |
| TAPS-1.2  | Done                                                                                                    |
| TAPS-1.3  | Done                                                                                                    |
| TAPS-1.4  | Done                                                                                                    |
| TAPS-1.5  | Done                                                                                                    |
| TAPS-1.6  | Done                                                                                                    |
| TAPS-1.7  | Done                                                                                                    |
| TAPS-1.8  | Done                                                                                                    |
| TAPS-1.9  | Done                                                                                                    |
| TAPS-1.10 | Done                                                                                                    |
| TAPS-1.11 | Ready (apps/web half-done as side effect of TAPS-3.2; apps/mobile pending)                              |
| TAPS-1.12 | Ready                                                                                                   |
| TAPS-1.13 | Ready                                                                                                   |
| TAPS-1.14 | Blocked (gh token 403 on branch-protection API; needs admin)                                            |
| TAPS-1.15 | Done                                                                                                    |
| TAPS-1.16 | Done                                                                                                    |
| TAPS-1.17 | Ready                                                                                                   |
| TAPS-1.18 | Ready                                                                                                   |
| TAPS-1.19 | Ready                                                                                                   |
| TAPS-1.20 | Done                                                                                                    |
| TAPS-1.21 | Done                                                                                                    |
| TAPS-2.1  | Done                                                                                                    |
| TAPS-2.2  | Done                                                                                                    |
| TAPS-2.3  | Done                                                                                                    |
| TAPS-2.4  | Done                                                                                                    |
| TAPS-2.5  | Ready                                                                                                   |
| TAPS-2.6  | Ready                                                                                                   |
| TAPS-2.7  | Ready                                                                                                   |
| TAPS-2.8  | Ready                                                                                                   |
| TAPS-2.10 | Ready (note: TAPS-2.9 is absent from the table — not a typo in this audit, the ID simply isn't present) |
| TAPS-2.11 | Ready                                                                                                   |
| TAPS-3.5  | Ready                                                                                                   |
| TAPS-3.6  | Ready                                                                                                   |
| TAPS-3.1  | Done                                                                                                    |
| TAPS-3.2  | Done                                                                                                    |
| TAPS-3.3  | Done                                                                                                    |
| TAPS-3.4  | Done                                                                                                    |

A "Sprint 2 Proposal" section (TAPS-3.1–3.4 full story text) is still present in the file marked "pending Ashish's sprint-planning approval — not started," even though the table above already marks all four of those same stories **Done**. This is a stale/contradictory section left in the doc, not a status-table error — worth cleaning up but does not misstate actual status.

**Spot-check of Done stories against real code (not exhaustive per-story proof):**

| Story        | Claim                                        | Code check                                                                                                                                                                                         | Result                                                                                                                |
| ------------ | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| TAPS-2.1     | Prisma schema for 6 content models           | `apps/api/prisma/schema.prisma` has `ExamBoard`, `Post`, `Syllabus`, `PastPaper`, `StudyMaterial`, `Book` with FKs/indexes as described                                                            | ✅ Confirmed                                                                                                          |
| TAPS-2.3     | Admin JWT CRUD for ExamBoard/Post            | `apps/api/src/auth/` (controller, service, JWT guard, DTOs) + `apps/api/src/exam-board/`, `apps/api/src/post/` exist with specs                                                                    | ✅ Confirmed                                                                                                          |
| TAPS-1.20    | `/health` endpoint                           | `apps/api/src/health/health.controller.ts` + spec exist; `fly.toml` health-checks `path = '/health'`                                                                                               | ✅ Confirmed                                                                                                          |
| TAPS-3.1     | Public read-only ExamBoard/Post API          | `apps/api/src/public-content/public-exam-boards.*`, `public-posts.*` controllers/services/specs exist                                                                                              | ✅ Confirmed                                                                                                          |
| TAPS-3.4     | Postgres full-text search (not Meilisearch)  | `apps/api/src/public-content/search.{controller,service}.ts` uses raw SQL `ts_rank`/`websearch_to_tsquery` against `searchVector` columns — matches architecture's "Postgres FTS initially" choice | ✅ Confirmed                                                                                                          |
| TAPS-3.2/3.3 | Web nav shell + exam hub pages               | `apps/web/src/components/nav/` (Header/Footer), `apps/web/src/app/exam-boards/[id]/` (incl. `not-found.test.tsx`) exist                                                                            | ✅ Confirmed                                                                                                          |
| TAPS-1.16    | Deploy (apps/api on Fly, apps/web on Vercel) | `fly.toml` present and configured; no `vercel.json` found (Vercel needs none for a default Next.js app — not a defect), README still describes deploy as _not_ set up (see §10)                    | ⚠️ Code/config confirms Fly deploy; **README text contradicts this Done story** — documentation drift, not a code gap |

No spot-checked "Done" story was found to be missing its underlying code.

---

## 4. Data Model

**Actual Prisma schema (`apps/api/prisma/schema.prisma`) — full model/field list:**

- **`ExamBoard`** (`@@map("exam_boards")`): `id` (cuid), `name` (unique), `type` (`ExamBoardType`: TEACHING/TET), `description` (Text), `createdAt`, `updatedAt`, relations `posts[]`, `syllabuses[]`, `pastPapers[]`, `searchVector` (`Unsupported("tsvector")`)
- **`Post`** (`@@map("posts")`): `id`, `examBoardId?`, `examBoard?` (relation, `onDelete: SetNull`), `type` (`PostType`: NOTIFICATION/ARTICLE), `title`, `slug` (unique), `body` (Text), `heroImage?`, `publishedAt?`, `createdAt`, `updatedAt`, `searchVector` (Unsupported tsvector); index on `examBoardId`
- **`Syllabus`** (`@@map("syllabuses")`): `id`, `examBoardId`, `examBoard` (relation, `onDelete: Restrict`), `subject`, `topics[]`, `createdAt`, `updatedAt`; indexes on `examBoardId`, `subject`
- **`PastPaper`** (`@@map("past_papers")`): `id`, `examBoardId`, `examBoard` (relation, `onDelete: Restrict`), `subject`, `year` (Int), `fileUrl`, `createdAt`, `updatedAt`; indexes on `examBoardId`, `subject`, `year`, composite `[examBoardId, subject, year]`
- **`StudyMaterial`** (`@@map("study_materials")`): `id`, `subject`, `title`, `fileUrl`, `createdAt`, `updatedAt`; index on `subject`
- **`Book`** (`@@map("books")`): `id`, `class`, `subject`, `title`, `fileUrl`, `createdAt`, `updatedAt`; indexes on `subject`, `class`

**Compared to `05-ARCHITECTURE.md` §4's 10-model list (`ExamBoard`, `Post`, `Syllabus`, `PastPaper`, `StudyMaterial`, `Book`, `User`, `QuizQuestion`, `QuizAttempt`, `StudyPlan`):**

- The 6 EPIC-2-owned content models are present and match the documented field lists, plus every model gains `createdAt`/`updatedAt` audit timestamps (explicitly called out and justified in the schema's own header comment and `docs/adr/004-content-schema-design.md`).
- `User`, `QuizQuestion`, `QuizAttempt`, `StudyPlan` (EPIC 4/5/7/8 territory) are **absent from the schema** — but this is explicitly flagged in the schema file's header comment as intentional/out-of-scope, and no backlog story claims them Done. **Not undocumented drift.**
- `searchVector` (tsvector generated columns on `ExamBoard`/`Post`) exists in the real DB/schema but is not listed in `05-ARCHITECTURE.md` §4 at all — added for TAPS-3.4 and documented in `docs/adr/009-full-text-search.md`, but §4 itself was never updated to mention it. **Minor documentation gap**, not a functional problem.

**Migrations present:** `20260910194813_init`, `20260910202809_examboard_name_unique`, `20260910215452_add_search_vectors` — names line up with the schema comments (unique-name constraint added for seed upserts, search vectors added for TAPS-3.4).

---

## 5. CI/CD

**`.github/workflows/ci.yml`** (only workflow file in the repo) — full contents:

```yaml
name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
      - name: Install dependencies (all workspaces)
        run: npm ci
      - name: Lint (every workspace with a lint script)
        run: npm run lint --workspaces --if-present
      - name: Test (every workspace with a test script)
        run: npm run test --workspaces --if-present
      - name: Format check
        run: npm run format:check
```

Note: this is still a single combined `lint-and-test` job — matches backlog `TAPS-1.18` ("Ready", not yet done) which proposes splitting it into per-app jobs.

**Last 5 workflow runs (`gh run list --limit 5`, `gh` authenticated as `Ashish-Bhatia`):**

| Status     | Title                                             | Branch                         | Event        | Run ID      | Duration | Time                 |
| ---------- | ------------------------------------------------- | ------------------------------ | ------------ | ----------- | -------- | -------------------- |
| ✅ success | Merge pull request #22 from Ashish-Bhatia/develop | main                           | push         | 34536593205 | 1m5s     | 2026-09-10T22:16:21Z |
| ✅ success | Sprint 2: EPIC 3 web nav, exam hub pages, search  | develop                        | pull_request | 34536475407 | 50s      | 2026-09-10T22:15:01Z |
| ✅ success | feat(TAPS-3.4): basic Postgres full-text search   | feature/TAPS-3-4-search        | pull_request | 34535776785 | 58s      | 2026-09-10T22:06:51Z |
| ✅ success | feat(TAPS-3.3): exam hub page template            | feature/TAPS-3-3-exam-hub-page | pull_request | 34534415420 | 53s      | 2026-09-10T21:51:25Z |
| ✅ success | feat(TAPS-3.2): apps/web navigation shell         | feature/TAPS-3-2-nav-shell     | pull_request | 34532669761 | 1m2s     | 2026-09-10T21:31:39Z |

All 5 most recent runs green.

---

## 6. Deploy Config

**`apps/api/fly.toml`** — full contents:

```toml
app = 'taps-api'
primary_region = 'sin'

[build]
  dockerfile = 'Dockerfile'

[env]
  PORT = '8080'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    interval = '15s'
    timeout = '5s'
    grace_period = '10s'
    method = 'GET'
    path = '/health'
    protocol = 'http'

[[vm]]
  memory = '256mb'
  cpus = 1
```

(Header comments document region choice — `sin` since Mumbai isn't a Fly platform region — and the required `--config`/`--dockerfile` deploy invocation from repo root.)

**`vercel.json`:** none found anywhere in the repo. Not a defect for a default Next.js app (Vercel auto-detects `apps/web` with zero config needed), but also means there's no committed record of the Vercel project's root-directory/build settings — those live only in the Vercel dashboard, undocumented in-repo beyond the runbook prose.

**Env var names referenced in code vs documented in `.env.example`:**

| Var                   | Referenced in code (`process.env.*`)                                                                                          | In `apps/api/.env.example`                             | In `apps/web/.env.example` | Actually set in `apps/api/.env`              |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | -------------------------- | -------------------------------------------- |
| `DATABASE_URL`        | used via Prisma's `env("DATABASE_URL")` in `schema.prisma` (not a literal `process.env.` grep hit, but is the datasource var) | ✅                                                     | —                          | ✅                                           |
| `ANTHROPIC_API_KEY`   | not referenced anywhere in `apps/api/src` (confirmed — not yet wired to an `AIService`)                                       | ✅ (explicitly noted as placeholder-ahead-of-EPIC-4/8) | —                          | ✅                                           |
| `PORT`                | ✅                                                                                                                            | ✅                                                     | —                          | ✅                                           |
| `ALLOWED_ORIGIN`      | ✅                                                                                                                            | ✅                                                     | —                          | ✅                                           |
| `JWT_SECRET`          | ✅                                                                                                                            | ✅                                                     | —                          | ✅                                           |
| `ADMIN_PASSWORD_HASH` | ✅                                                                                                                            | ✅                                                     | —                          | ✅                                           |
| `NEXT_PUBLIC_API_URL` | ✅ (`apps/web`)                                                                                                               | —                                                      | ✅                         | n/a (web has no `.env` checked in, expected) |

No drift: every env var referenced in code is documented in the matching app's `.env.example`, and the real `apps/api/.env` (present locally, correctly gitignored) has exactly the same key set as `.env.example` — no extra, no missing keys.

---

## 7. Secrets Hygiene

Scanned both the current tracked tree (`git grep`) and the **full git history** (`git log --all -p`) for common secret patterns: Anthropic keys (`sk-ant-api*`), AWS access keys (`AKIA*`), GitHub tokens (`ghp_`/`gho_`/`ghu_`), Slack tokens (`xox[baprs]-`), PEM private key headers, and inline `user:password@host` credentials in `postgres://` URLs.

**Findings — both in a template/placeholder context, no real secret material:**

| File                    | Line | Pattern (redacted)                                                                      | Assessment                                                                                   |
| ----------------------- | ---- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `apps/api/.env.example` | 12   | `postgresql://<user>:<redacted>@<project>-pooler.<region>.aws.neon.tech/<database>?...` | Literal `<user>`/`<password>` placeholders, not a real credential                            |
| `apps/api/.env.example` | 18   | `ANTHROPIC_API_KEY=sk-ant-api03-<redacted>`                                             | File literally spells `sk-ant-api03-replace-with-a-real-key` — a placeholder, not a real key |

**Full git history** (`git log --all -p`) surfaced only one match total, the same `.env.example` placeholder line at the commit that introduced it — no other commit, including squashed/deleted branches reachable via `--all`, contains a matching pattern.

**`apps/api/.env` (the real local env file):** confirmed gitignored (`.gitignore:19` covers `.env` and `apps/api/.env`) and confirmed **not tracked** (`git ls-files apps/api/.env` returns nothing). No real secret values were printed by this audit — key _names_ only were checked (§6).

**Conclusion: no leaked secrets found**, in the current tree or in history.

---

## 8. Dependencies

**Per-workspace `package.json` dependency lists:**

- **`apps/api`** — deps: `@nestjs/common`, `@nestjs/core`, `@nestjs/jwt`, `@nestjs/mapped-types`, `@nestjs/observe`, `@nestjs/platform-express`, `@prisma/client@6.19.3`, `bcryptjs@3.0.3`, `class-transformer`, `class-validator`, `reflect-metadata`, `rxjs`. Dev: `@nestjs/cli`, `@nestjs/mau`, `@nestjs/schematics`, `@nestjs/testing`, ESLint stack, `prisma@6.19.3`, `vitest@^4.1.2`, etc.
- **`apps/web`** — deps: `next@16.3.4`, `react@19.2.3`, `react-dom@19.2.3`. Dev: Tailwind 4, Testing Library, `vitest@5.0.0`, `jsdom`, ESLint/`eslint-config-next`.
- **`apps/mobile`** — deps: `expo@~57.0.21`, `expo-status-bar`, `react@19.2.3`, `react-native@0.86.3`. Dev: `eslint-config-expo`, TypeScript.
- **`packages/types`, `packages/ui`** — no `package.json`; empty (see §2).

**`npm outdated --workspaces` highlights (not urgent, informational):** `@prisma/client`/`prisma` 6.19.3 → 7.10.0/8.0.0-rc available (backlog `TAPS-2.5` already tracks this); `eslint` 9.39.5 → 10.x available; `vitest`/`@vitest/coverage-v8` (api) 4.1.11 → 5.0.0 available (note: `apps/web` already runs vitest 5.0.0 — a version mismatch between workspaces); `typescript` 6.0.x (api/mobile) vs 5.9.3 (web) — inconsistent TS major version across workspaces.

**`npm audit --audit-level=high --workspaces` (read-only, no fix applied): 23 total vulnerabilities (10 high, 11 moderate, 2 low).**

High-severity findings:

| Package                 | Advisory                                                                                                     | Path                                                                                | Fix available?                                                                                                                                                                |
| ----------------------- | ------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `deepmerge-ts` (<8.0.0) | GHSA-ggr8-5vv4-36mx (stack exhaustion)                                                                       | via `@prisma/config` → `prisma` (devDependency)                                     | `npm audit fix --force` → downgrades `prisma` to 6.12.0 (breaking) — **already tracked as `TAPS-2.8`**, correctly noted there as "devDependency-only, build-time not runtime" |
| `multer` (≤2.2.0)       | 4 DoS/bypass advisories (GHSA-wc9g-mqfw-jrwm, GHSA-qfvm-cv95-jqjf, GHSA-qvfw-j98x-7q72, GHSA-535w-7cp7-47q4) | via `@nestjs/platform-express` → `@nestjs/core`/`@nestjs/testing`/`@nestjs/observe` | **No fix available** upstream yet                                                                                                                                             |
| `tmp` (≤0.2.5)          | GHSA-52f5-9888-hmc6, GHSA-ph9p-34f9-6g65 (symlink write / path traversal)                                    | via `external-editor` → `inquirer` → `@nestjs/mau` (devDependency, CLI tooling)     | `npm audit fix --force` → downgrades `@nestjs/mau` to 0.0.6 (breaking)                                                                                                        |
| `undici` (≤6.27.0)      | 14 advisories (request smuggling, DoS, header/cookie injection, etc.)                                        | via the same `@nestjs/mau` devDependency chain                                      | `npm audit fix --force` → same breaking downgrade as above                                                                                                                    |

Moderate: `uuid` (<11.1.1) via `apps/mobile`'s Expo toolchain (`xcode`→`@expo/config-plugins`→`@expo/cli`→`expo`), fix requires downgrading `expo` to 46.0.21 (breaking) — not currently tracked as a backlog story.

**Net assessment:** the `multer` finding is the only high-severity issue that reaches a **runtime** dependency path (`@nestjs/platform-express`, used by the live API); the rest are devDependency/build-tool-only chains. `multer` has no upstream fix yet and isn't currently in the backlog — worth a story. `deepmerge-ts` is already correctly tracked (`TAPS-2.8`). This audit did not run `npm audit fix` (read-only, as instructed).

---

## 9. Tests

**Test files found (23 total):**

`apps/api` (18 spec files): `prisma/exam-boards.seed-data.spec.ts`, `src/app.controller.spec.ts`, `src/auth/auth.controller.spec.ts`, `src/auth/auth.service.spec.ts`, `src/auth/jwt-auth.guard.spec.ts`, `src/common/prisma-exception.filter.spec.ts`, `src/exam-board/exam-board.controller.spec.ts`, `src/exam-board/exam-board.service.spec.ts`, `src/health/health.controller.spec.ts`, `src/post/post.controller.spec.ts`, `src/post/post.service.spec.ts`, `src/prisma/prisma.service.spec.ts`, `src/public-content/public-exam-boards.controller.spec.ts`, `src/public-content/public-exam-boards.service.spec.ts`, `src/public-content/public-posts.controller.spec.ts`, `src/public-content/public-posts.service.spec.ts`, `src/public-content/search.controller.spec.ts`, `src/public-content/search.service.spec.ts`

`apps/web` (5 test files): `src/app/exam-boards/[id]/not-found.test.tsx`, `src/components/nav/Footer.test.tsx`, `src/components/nav/Header.test.tsx`, `src/components/ui/ComingSoon.test.tsx`, `src/lib/api.test.ts`

`apps/mobile`: no test files, no `test` script in `package.json` — matches backlog `TAPS-1.11` ("apps/mobile still pending").

**Test run results (read-only, `npm run test`, no fixes attempted):**

- **`apps/api`**: ✅ **18 test files passed, 52 tests passed**, 0 failed. (17.75s). A benign `ECONNREFUSED` line is logged by `PrismaService` during the `prisma.service.spec.ts` run — this is the service's own documented lazy-retry behavior when no live DB is reachable in the test sandbox, not a test failure; the suite still passed.
- **`apps/web`**: ✅ **5 test files passed, 16 tests passed**, 0 failed. (11.01s). A non-fatal Vite warning about `configLoader: 'native'`/CommonJS `vitest.config.ts` was printed; no effect on results.
- **`apps/mobile`**: no test script to run.

**Total: 68/68 tests passing across the two workspaces that have test suites.**

---

## 10. Docs Completeness

**`docs/adr/` (9 ADRs):** `001-monorepo-tooling.md`, `002-husky-prepare-guard.md`, `003-prisma-orm-and-connection-strategy.md`, `004-content-schema-design.md`, `005-admin-auth-and-validation.md`, `006-public-content-api-shape.md`, `007-nav-data-sourcing.md`, `008-web-api-fetch-caching.md`, `009-full-text-search.md`

**`docs/runbooks/` (3):** `database-migrations.md`, `database-seeding.md`, `deploy-api.md`

**`docs/sprints/` (2):** `sprint-00-verification.md`, `sprint-01-summary.md` — **no `sprint-02-summary.md`**, even though Sprint 2 (`TAPS-3.1`–`3.4`) is fully marked Done in the backlog. Per `07-DOCUMENTATION-STANDARDS.md`'s table ("Sprint ends → `/docs/sprints/sprint-NN-summary.md`"), this is expected only once Sprint 2 is formally closed out — since the backlog itself never records a Sprint 2 close-out event (only the still-open "Sprint 2 Proposal" section noted in §3), this reads as an **in-progress sprint close-out gap** rather than a missed one, but is worth flagging since all 4 of that sprint's stories already show Done.

**Cross-check — every Done Sprint 1/2 story has matching doc impact recorded and files present:** `TAPS-2.1`→`docs/architecture/data-model.md` + ADR 004 ✅; `TAPS-2.2`→`docs/runbooks/database-migrations.md` + ADR 003 ✅; `TAPS-2.3`→`docs/api/{auth,exam-boards,posts}.md` + ADR 005 ✅; `TAPS-2.4`→`docs/runbooks/database-seeding.md` ✅; `TAPS-3.1`→`docs/api/public-content.md` + ADR 006 ✅; `TAPS-3.2`→`docs/architecture/web-app.md` + ADR 007 ✅; `TAPS-3.3`→`docs/architecture/web-app.md`, ADR 008 ✅; `TAPS-3.4`→ADR 009, `docs/api/public-content.md`, `docs/architecture/web-app.md` ✅. All doc-impact commitments in the backlog resolve to real files.

**Root `README.md` — two stale claims found**, against `07-DOCUMENTATION-STANDARDS.md`'s explicit "must never go stale" / "Claude updates this every sprint" requirement:

1. `## Current sprint` (line 39) still reads **"Sprint 0"** — the repo is actually well past Sprint 1 (closed) and into Sprint 2, whose 4 stories are all Done.
2. `## Deployment` (lines 34–35) states _"Web/mobile deploy targets aren't set up yet (`TAPS-1.16` in the backlog)"_ — but `TAPS-1.16` is recorded **Done** in the backlog (apps/api live on Fly, apps/web live on Vercel Production, verified with a live curl per `docs/runbooks/deploy-api.md`).
3. (Minor, same section) Line 25's test comment — _"Vitest (apps/api); more apps get test scripts as they gain code"_ — is also stale: `apps/web` already has a `test` script and 5 passing test files (§9).

These are documentation-standards violations against the repo's own house rules, not code defects.

---

## 11. Loose Ends

Searched `apps/`, `packages/`, `infra/`, `.github/` (source + markdown, excluding `node_modules`/`dist`/`.next`) for `TODO`, `FIXME`, `HACK`, `XXX`, and placeholder/dummy/stub/not-implemented style comments.

**Result: none found.** No `TODO`/`FIXME`/`HACK`/`XXX` markers and no "placeholder value" / "dummy data" / "stub implementation" / "not (yet) implemented" comments exist anywhere in the current tracked source tree. The codebase's own convention (visible throughout the ADRs and backlog) is to track loose ends as explicit backlog stories (e.g. `TAPS-2.10`, `TAPS-3.5`, `TAPS-3.6`) rather than inline code comments — that pattern holds up under this scan.

---

## Summary of Findings

| #   | Severity | Finding                                                                                                                                                                                                                |
| --- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Medium   | Root `README.md` "Current sprint" says Sprint 0; actual state is past Sprint 1, into Sprint 2 (all 4 stories Done) — violates the repo's own doc standard that README must never go stale (§10).                       |
| 2   | Medium   | Root `README.md` Deployment section says web/mobile deploy isn't set up and cites `TAPS-1.16` as the blocker, but `TAPS-1.16` is recorded Done (Fly + Vercel both live) (§10).                                         |
| 3   | Low      | `docs/backlog/BACKLOG.md` still carries a "Sprint 2 Proposal ... not started" section for TAPS-3.1–3.4, even though the status table above it already marks all four Done — stale/contradictory leftover section (§3). |
| 4   | Low      | `multer` high-severity DoS advisories reach a runtime dependency path (`@nestjs/platform-express`) with no upstream fix yet available; not currently tracked as a backlog story (§8).                                  |
| 5   | Low      | `05-ARCHITECTURE.md` §4's data model list doesn't mention the `searchVector` (tsvector) columns added by TAPS-3.4, even though `docs/adr/009-full-text-search.md` documents them (§4).                                 |
| 6   | Info     | `docs/sprints/` has no `sprint-02-summary.md` yet, though Sprint 2's stories are all Done — the backlog never records a formal Sprint 2 close-out event to trigger it (§10).                                           |
| 7   | Info     | Inconsistent tool versions across workspaces: TypeScript 6.0.x (api/mobile) vs 5.9.3 (web); Vitest 4.1.11 (api) vs 5.0.0 (web) (§8).                                                                                   |
| 8   | Info     | Local `main` branch ref is 40 commits behind `origin/main` (stale local checkout only — `origin/main`/`develop` are in sync) (§1).                                                                                     |

**No secrets leaked, no failing tests, no CI failures, no repo-structure drift, no TODO/FIXME/placeholder debt, and every spot-checked "Done" story has real backing code.** The two README staleness items (#1, #2) are the most actionable findings — a direct violation of a standard this project holds itself to explicitly.

---

_Generated by a read-only audit pass. No files were modified other than the creation of this report._
